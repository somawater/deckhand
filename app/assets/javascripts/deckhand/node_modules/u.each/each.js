var is = require('u.is');

module.exports = function (list, callback) {
  if (is.array(list)) {
    for (var it = 0, len = list.length; it < len; it++) {
      callback(list[it], it);
    }
  } else if (is.object(list)) {
    for (var prop in list) {
      callback(list[prop], prop);
    }
  }
};
